<!DOCTYPE html>
<html>
  <head>
    <title>Bookers</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <script src="https://kit.fontawesome.com/a2b7a944cd.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="d-flex flex-column vh-100">
     <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">

        <nav class="nav navbar-nav navbar-left">
          <span>Bookers</span>
        </nav>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><%= link_to ' Home', user_path(current_user), class: 'fa-solid fa-house' %></li>
              <li><%= link_to ' Users', users_path, class: 'fa-solid fa-user' %></li>
              <li><%= link_to ' Books', books_path, class: 'fa-solid fa-book' %></li>
              <li><%= link_to ' logout', destroy_user_session_path, method: :delete, class: 'fa-solid fa-arrow-right-to-bracket' %></li>
            <% else %>
              <li><%= link_to ' Home', root_path, class: 'fa-solid fa-house' %></li>
              <li><%= link_to ' About', homes_about_path, class: 'fa-solid fa-link' %></li>
              <li><%= link_to ' sign up', new_user_registration_path, class: 'fa-solid fa-user-plus' %></li>
              <li><%= link_to ' login', new_user_session_path, class: 'fa-solid fa-arrow-right-to-bracket' %></li>
            <% end %>
          </ul>
        </nav>

      </div>
    </header>
    <div class="body">
      <p class="notice"><%= notice %></p>

      <%= yield %>


        <footer>
          <p class="text-right"> CopyRight Infratop.inc</p>
        </footer>
      </div>
    </div>
  </body>
</html>